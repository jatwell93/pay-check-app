---
phase: 01-api-foundation-award-selection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/awardRatesService.js
  - src/services/awardRatesService.test.js
  - .env.example
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03

must_haves:
  truths:
    - "Award rates for MA000012, MA000003, MA000009 can be fetched from the FWC API and returned as a structured map"
    - "Fetched rates are stored in localStorage with versioned keys and a 90-day expiry timestamp"
    - "On a second call, getCachedAwardRates returns the stored data without calling the API if TTL has not expired"
    - "On a cache miss or expired TTL, getCachedAwardRates returns null"
    - "API call failures trigger retry with exponential backoff (up to 3 retries, network errors + 5xx only)"
    - "Zod validation rejects malformed API responses before anything is stored"
    - "Corrupted localStorage JSON (parse failure) is handled gracefully — entry deleted, null returned"
  artifacts:
    - path: "src/services/awardRatesService.js"
      provides: "FWC API client, caching logic, Zod validation"
      exports:
        - fetchAwardRates
        - getCachedAwardRates
        - getLastCacheUpdateTime
        - clearCache
    - path: "src/services/awardRatesService.test.js"
      provides: "Unit tests covering cache hit, cache miss, TTL expiry, API fetch, fallback"
      min_lines: 60
    - path: ".env.example"
      provides: "Documents REACT_APP_FWC_API_KEY for developer onboarding"
      contains: "REACT_APP_FWC_API_KEY"
  key_links:
    - from: "src/services/awardRatesService.js"
      to: "localStorage"
      via: "createCacheKey(awardId) produces award_rates_v1_{awardId}"
      pattern: "award_rates_v1_"
    - from: "src/services/awardRatesService.js"
      to: "FWC API"
      via: "axios instance with axiosRetry, REACT_APP_FWC_API_KEY header"
      pattern: "REACT_APP_FWC_API_KEY"
---

<objective>
Build the awardRatesService module: the FWC API client, localStorage cache layer, and Zod schema validation.

Purpose: Every downstream part of Phase 1 and Phase 2 relies on this service as the single source of award rate data. Getting it right — with proper caching, TTL expiry, validation, and fallback — is the foundation the rest of the app builds on.

Output: src/services/awardRatesService.js (exported API), src/services/awardRatesService.test.js (unit tests), .env.example (developer setup doc).
</objective>

<execution_context>
@C:/Users/josha/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/josha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-api-foundation-award-selection/01-CONTEXT.md
@.planning/phases/01-api-foundation-award-selection/01-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs. No codebase exploration required. -->

From src/App.js — pharmacyAwardRates (fallback data structure reference only):

  fullTimePartTime: { 'pharmacy-assistant-1': { base: 25.99 }, ... }
  casual: { 'pharmacy-assistant-1': { base: 32.49 }, ... }
  juniorPercentages: { 'under-16': 0.45, ... }
  allowances: { homeMedicineReview: 17.96, ... }

The service module does NOT need to replicate this structure for API responses.
App.js holds pharmacyAwardRates as the hardcoded fallback only.

Service module must export (plain JS, not TypeScript):
  fetchAwardRates(awardIds)         — async, fetches and caches all awards
  getCachedAwardRates(awardId)      — sync, returns cached data or null
  getLastCacheUpdateTime(awardId)   — sync, returns Date or null
  clearCache(awardId)               — sync, clears one or all versioned cache keys
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dependencies and write test scaffold</name>
  <files>package.json, src/services/awardRatesService.test.js, src/setupTests.js</files>
  <behavior>
    - Test: getCachedAwardRates returns null when localStorage has no entry for awardId
    - Test: getCachedAwardRates returns null when cached entry is expired (expiry timestamp in the past)
    - Test: getCachedAwardRates returns the stored data object when cache is valid (expiry in the future)
    - Test: getCachedAwardRates returns null and removes the item when JSON.parse throws (corrupted entry)
    - Test: getLastCacheUpdateTime returns a Date matching the stored timestamp, or null if no cache
    - Test: clearCache(awardId) removes only the specific versioned key from localStorage
    - Test: clearCache() with no argument removes all keys matching the award_rates_v1_ prefix
    - Test: fetchAwardRates calls the FWC API for each awardId and stores validated results in cache
    - Test: fetchAwardRates throws when Zod validation fails (malformed response shape)
    - Test: fetchAwardRates surfaces network errors to the caller (not silently swallowed)
  </behavior>
  <action>
    Step 1: Run npm install axios axios-retry zod

    Step 2: Create src/services/awardRatesService.test.js with the above 10 test cases as RED (failing) stubs.
    - Mock axios using jest.mock('axios') to avoid real HTTP calls
    - Mock localStorage inline in the test file using beforeEach/afterEach with a plain in-memory object — the existing setupTests.js has no localStorage mock, so add it here:
        let store = {};
        beforeEach(() => {
          store = {};
          jest.spyOn(Storage.prototype, 'getItem').mockImplementation(k => store[k] ?? null);
          jest.spyOn(Storage.prototype, 'setItem').mockImplementation((k, v) => { store[k] = v; });
          jest.spyOn(Storage.prototype, 'removeItem').mockImplementation(k => { delete store[k]; });
          jest.spyOn(Storage.prototype, 'key').mockImplementation(i => Object.keys(store)[i] ?? null);
          jest.spyOn(Storage.prototype, 'clear').mockImplementation(() => { store = {}; });
          Object.defineProperty(Storage.prototype, 'length', { get: () => Object.keys(store).length });
        });
        afterEach(() => jest.restoreAllMocks());
    - All 10 tests should FAIL at this point because awardRatesService.js does not yet exist
    - Do NOT write the implementation in this task

    Step 3: Run npm test -- --watchAll=false --testPathPattern=awardRatesService
    Confirm tests fail (RED phase confirmed).

    Nyquist note: This task creates the test scaffold. Task 2 implements the service and makes tests pass (GREEN).
  </action>
  <verify>
    <automated>npm test -- --watchAll=false --testPathPattern=awardRatesService 2>&1 | tail -20</automated>
  </verify>
  <done>Test file src/services/awardRatesService.test.js exists with 10+ test cases. All tests fail with "Cannot find module" or similar — RED phase confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement awardRatesService.js (GREEN phase)</name>
  <files>src/services/awardRatesService.js, .env.example</files>
  <behavior>
    - All 10 test cases from Task 1 pass after this implementation
    - Cache key format: award_rates_v1_{awardId} (e.g. award_rates_v1_MA000012)
    - Cache entry shape stored in localStorage: { data: ..., timestamp: Date.now(), expiry: Date.now() + TTL_MS }
    - TTL: 90 days in milliseconds (90 * 24 * 60 * 60 * 1000)
    - axios instance: baseURL from process.env.REACT_APP_FWC_API_BASE_URL, fallback to https://api.fwc.gov.au, timeout 10000ms
    - API key from process.env.REACT_APP_FWC_API_KEY — sent as x-api-key header; omit header entirely if env var is empty
    - axiosRetry: retries 3, exponentialDelay, retryCondition only on network errors or 5xx (not on 4xx)
    - Zod schema: permissive passthrough — validate at minimum that response is a non-null object (tighten in Phase 2 once real FWC schema confirmed)
    - fetchAwardRates: fetches all awardIds in parallel with Promise.all, validates each with Zod, caches each, returns map
    - On Zod failure: throw new Error('Unexpected award rate data format from FWC API')
    - On network failure: re-throw original error (not swallowed)
    - getCachedAwardRates: wraps JSON.parse in try/catch — on failure removes item and returns null
    - All exports are plain JavaScript (no TypeScript syntax — this is a CRA JavaScript project)
  </behavior>
  <action>
    Create src/services/awardRatesService.js implementing all behaviors above.

    The module structure:
    1. Import axios, axiosRetry, and z from zod
    2. Define constants: CACHE_KEY_PREFIX = 'award_rates_v1', CACHE_TTL_MS = 90 * 24 * 60 * 60 * 1000
    3. Define createCacheKey(awardId) helper: returns CACHE_KEY_PREFIX + '_' + awardId
    4. Create axios instance with baseURL and timeout; configure axiosRetry on it
    5. Define FWC_AWARD_SCHEMA using z.object({}).passthrough() — minimal validation until Phase 2 confirms real schema shape; add code comment: "TODO Phase 2: tighten schema once real FWC API response shape is confirmed"
    6. Implement and export fetchAwardRates(awardIds) — async
    7. Implement and export getCachedAwardRates(awardId) — sync
    8. Implement and export getLastCacheUpdateTime(awardId) — sync
    9. Implement and export clearCache(awardId) — sync; when called with no argument, iterate Object.keys(localStorage) and remove all keys starting with CACHE_KEY_PREFIX

    After writing the implementation, run: npm test -- --watchAll=false --testPathPattern=awardRatesService
    All 10 tests must pass (GREEN).

    Create .env.example at the project root (C:/Users/josha/pay-check-app/.env.example):
      # FWC Modern Awards Pay Database API Key
      # Obtain from developer.fwc.gov.au
      REACT_APP_FWC_API_KEY=your_api_key_here

      # Optional: override FWC API base URL (defaults to https://api.fwc.gov.au)
      # REACT_APP_FWC_API_BASE_URL=https://api.fwc.gov.au

    Note: do NOT create .env.local — that is for the user to create with their real key. CRA projects gitignore .env.local by default.
  </action>
  <verify>
    <automated>npm test -- --watchAll=false --testPathPattern=awardRatesService</automated>
  </verify>
  <done>All awardRatesService tests pass (GREEN). Service exports fetchAwardRates, getCachedAwardRates, getLastCacheUpdateTime, clearCache. .env.example exists at project root with REACT_APP_FWC_API_KEY documented.</done>
</task>

</tasks>

<verification>
Full test suite after this plan:
  npm test -- --watchAll=false --testPathPattern=awardRatesService

All awardRatesService tests pass. Existing App.test.js still passes (App.js is not modified in this plan).
</verification>

<success_criteria>
- src/services/awardRatesService.js exists with 4 named exports: fetchAwardRates, getCachedAwardRates, getLastCacheUpdateTime, clearCache
- src/services/awardRatesService.test.js has 10+ test cases, all passing
- .env.example documents REACT_APP_FWC_API_KEY at project root
- axios, axios-retry, zod are in package.json dependencies
- Cache keys follow pattern award_rates_v1_{awardId}
- TTL is 90 days, lazy expiry checked on read
- fetchAwardRates validates responses with Zod before caching
</success_criteria>

<output>
After completion, create `.planning/phases/01-api-foundation-award-selection/01-01-SUMMARY.md`
</output>
