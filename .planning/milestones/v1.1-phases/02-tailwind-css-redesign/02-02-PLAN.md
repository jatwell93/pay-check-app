---
phase: 02-tailwind-css-redesign
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/components/AwardSelector.js
  - src/components/EmployeeDetails.js
  - src/components/Allowances.js
  - src/components/AwardSelector.test.js
autonomous: true
requirements:
  - UX-01

must_haves:
  truths:
    - "AwardSelector renders with a styled select dropdown, timestamp, and Refresh button; its internal error div is removed (error banner is now in App.js)"
    - "EmployeeDetails renders classification/employment type/age selects and inputs with the standard input class string"
    - "Allowances renders checkboxes with accent-blue-600, and number inputs with the standard input class string"
    - "All three components are fully contained in white/gray-50 card panels with rounded corners and shadows"
  artifacts:
    - path: "src/components/AwardSelector.js"
      provides: "Styled award selector section"
      exports: ["AwardSelector"]
    - path: "src/components/EmployeeDetails.js"
      provides: "Styled employee details form"
      exports: ["EmployeeDetails"]
    - path: "src/components/Allowances.js"
      provides: "Styled allowances form"
      exports: ["Allowances"]
  key_links:
    - from: "src/components/EmployeeDetails.js inputs"
      to: "UI-SPEC Form Styling Contract"
      via: "w-full p-3 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-transparent class string"
      pattern: "focus:ring-2 focus:ring-blue-500"
    - from: "src/components/Allowances.js checkboxes"
      to: "UI-SPEC Form Styling Contract"
      via: "w-4 h-4 accent-blue-600 cursor-pointer class string"
      pattern: "accent-blue-600"
---

<objective>
Redesign the three form-area components — AwardSelector, EmployeeDetails, and Allowances — with Tailwind CSS utility classes per the UI-SPEC Form Styling Contract. These three components are independent of each other and can be converted in any order.

Purpose: These components are the primary data-entry UI. Applying the consistent input/select/label/card styling makes the form professional and touch-friendly (p-3 inputs meet 44px touch target requirement).

Output: Three components fully converted to Tailwind utilities. No old CSS class names (.award-selector, .button, etc.) remaining. AwardSelector's own error div removed (error is now the App.js banner from Plan 01).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-tailwind-css-redesign/02-CONTEXT.md
@.planning/phases/02-tailwind-css-redesign/02-UI-SPEC.md
@.planning/phases/02-tailwind-css-redesign/02-01-SUMMARY.md
</context>

<interfaces>
<!-- Standard form input class string (from UI-SPEC Form Styling Contract): -->
<!-- inputs (text/number/time):                                               -->
<!--   "w-full p-3 border border-gray-300 rounded-md focus:ring-2            -->
<!--    focus:ring-blue-500 focus:border-transparent                          -->
<!--    placeholder-gray-400 text-gray-700"                                   -->
<!--                                                                          -->
<!-- select dropdowns:                                                        -->
<!--   "w-full p-3 border border-gray-300 rounded-md focus:ring-2            -->
<!--    focus:ring-blue-500 focus:border-transparent text-gray-700 bg-white"  -->
<!--                                                                          -->
<!-- checkboxes (Allowances):                                                 -->
<!--   "w-4 h-4 accent-blue-600 cursor-pointer"                              -->
<!--                                                                          -->
<!-- label class string:                                                      -->
<!--   "block text-sm font-medium text-gray-700 mb-1"                        -->
<!--                                                                          -->
<!-- card/section panel wrapper:                                              -->
<!--   "bg-white border border-gray-200 rounded-md shadow-sm p-4"            -->
<!--                                                                          -->
<!-- section heading (h2):                                                    -->
<!--   "text-xl font-semibold mb-4 text-gray-800 border-b border-gray-200 pb-2" -->
<!--                                                                          -->
<!-- AwardSelector props (unchanged — do not remove any props):              -->
<!-- { selectedAward, onSelectAward, awardMetadata, isLoading, error,        -->
<!--   lastUpdated, onRefresh, successMessage }                               -->
<!--                                                                          -->
<!-- EmployeeDetails props (unchanged):                                       -->
<!-- { classification, setClassification, employmentType, setEmploymentType, -->
<!--   age, setAge, customRate, setCustomRate, classifications, ageOptions,   -->
<!--   juniorClassificationIds }                                               -->
<!--                                                                          -->
<!-- Allowances props (unchanged):                                            -->
<!-- { allowances, handleAllowanceChange, classification,                     -->
<!--   allowanceConfig, classifications }                                      -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Redesign AwardSelector component</name>
  <files>src/components/AwardSelector.js, src/components/AwardSelector.test.js</files>
  <read_first>
    - C:/Users/josha/pay-check-app/src/components/AwardSelector.js (full current file)
    - C:/Users/josha/pay-check-app/src/components/AwardSelector.test.js (existing tests — must not break)
    - C:/Users/josha/pay-check-app/.planning/phases/02-tailwind-css-redesign/02-UI-SPEC.md (Form Styling Contract, Layout Contract — Error Banner section)
  </read_first>
  <behavior>
    - Existing AwardSelector.test.js tests must continue to pass
    - NEW test: The error div inside AwardSelector is gone; error prop is accepted but AwardSelector does NOT render its own error element (error UI is App.js banner per D-09)
    - NEW test: successMessage renders with green text when provided
    - NEW test: The refresh button is disabled and shows "Refreshing..." when isLoading is true
  </behavior>
  <action>
**First, read AwardSelector.test.js** and add the new tests noted in the behavior section.

**Then rewrite AwardSelector.js** to:

1. Keep all existing props (selectedAward, onSelectAward, awardMetadata, isLoading, error, lastUpdated, onRefresh, successMessage)
2. Remove the `{error && <div className="award-selector__error">...</div>}` block — error display is now the App.js banner. The `error` prop stays in the signature for API compatibility but is not rendered.
3. Remove all BEM class names (.award-selector, .award-selector__row, .award-selector__select, .award-selector__refresh-btn, .award-selector__timestamp, .award-selector__success)
4. Apply Tailwind classes:

```jsx
import React from 'react';
import { formatDistanceToNow } from 'date-fns';

function AwardSelector({
  selectedAward,
  onSelectAward,
  awardMetadata,
  isLoading,
  error,
  lastUpdated,
  onRefresh,
  successMessage,
}) {
  return (
    <div className="mb-6 bg-white border border-gray-200 rounded-md shadow-sm p-4">
      <div className="flex flex-wrap items-center gap-3">
        <label htmlFor="award-select" className="text-sm font-medium text-gray-700 whitespace-nowrap">
          Award:
        </label>
        <select
          id="award-select"
          value={selectedAward}
          onChange={(e) => onSelectAward(e.target.value)}
          disabled={isLoading}
          className="flex-1 min-w-0 p-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-transparent text-gray-700 bg-white disabled:bg-gray-100 disabled:cursor-not-allowed"
        >
          {Object.entries(awardMetadata).map(([awardId, meta]) => (
            <option key={awardId} value={awardId}>
              {meta.name}
            </option>
          ))}
        </select>
        <button
          onClick={onRefresh}
          disabled={isLoading}
          className="px-4 py-2 bg-slate-700 text-white text-sm font-medium rounded-md hover:bg-slate-800 disabled:bg-gray-400 disabled:cursor-not-allowed whitespace-nowrap"
        >
          {isLoading ? 'Refreshing...' : 'Refresh Rates'}
        </button>
      </div>

      {lastUpdated && (
        <p className="text-xs text-gray-500 mt-2">
          Rates last updated {formatDistanceToNow(lastUpdated, { addSuffix: true })}
        </p>
      )}

      {successMessage && (
        <p className="text-xs text-green-600 font-medium mt-2">{successMessage}</p>
      )}
    </div>
  );
}

export default AwardSelector;
```

Run `npm test -- --watchAll=false --testPathPattern="AwardSelector.test"` — all tests including new ones should pass (GREEN).
  </action>
  <verify>
    <automated>npm test -- --watchAll=false --testPathPattern="AwardSelector.test.js" 2>&1 | tail -20</automated>
  </verify>
  <done>All AwardSelector.test.js tests pass; AwardSelector.js contains no BEM class names (.award-selector, .award-selector__row etc.); AwardSelector.js contains "focus:ring-2 focus:ring-blue-500"; AwardSelector.js does NOT contain a rendered error div (error prop accepted but not rendered).</done>
</task>

<task type="auto">
  <name>Task 2: Redesign EmployeeDetails and Allowances components</name>
  <files>src/components/EmployeeDetails.js, src/components/Allowances.js</files>
  <read_first>
    - C:/Users/josha/pay-check-app/src/components/EmployeeDetails.js (current — already has partial Tailwind)
    - C:/Users/josha/pay-check-app/src/components/Allowances.js (current — already has partial Tailwind)
    - C:/Users/josha/pay-check-app/src/components/EmployeeDetails.test.js (existing tests — must not break)
    - C:/Users/josha/pay-check-app/src/components/Allowances.test.js (existing tests — must not break)
    - C:/Users/josha/pay-check-app/.planning/phases/02-tailwind-css-redesign/02-UI-SPEC.md (Form Styling Contract)
  </read_first>
  <action>
**EmployeeDetails.js** — update the following class strings (props interface and logic are unchanged):

Card wrapper (outermost div): change `"md:col-span-1 p-4 border rounded-md bg-gray-50"` to:
`"md:col-span-2 bg-white border border-gray-200 rounded-md shadow-sm p-4"`

Note: EmployeeDetails should span 2 columns in the 3-col grid (it has more fields than Allowances). Allowances spans 1 column.

Section heading (h2): change `"text-xl font-semibold mb-4 text-blue-700"` to:
`"text-xl font-semibold mb-4 text-gray-800 border-b border-gray-200 pb-2"`

All `<label>` elements (except inline radio labels): change to `"block text-sm font-medium text-gray-700 mb-1"`

All `<select>` dropdowns (classification, age): change to:
`"w-full p-3 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-transparent text-gray-700 bg-white"`

Add `disabled:bg-gray-100 disabled:text-gray-400 disabled:cursor-not-allowed` to the age select (it's conditionally disabled).

Custom rate `<input type="number">`: change to:
`"w-full p-3 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-transparent placeholder-gray-400 text-gray-700"`

Employment type radio group container `<div className="flex space-x-4">`: change to `"flex flex-wrap gap-4"`

Each radio option `<label>`: change to `"flex items-center gap-2 text-sm text-gray-700 cursor-pointer"`

Each `<input type="radio">`: change from `"mr-2"` to `"accent-blue-600 cursor-pointer"`

Junior rates note `<p>`: change from `"text-xs text-gray-500 mt-1"` to `"text-xs text-gray-400 mt-1"` (no change needed — already correct)

---

**Allowances.js** — update the following class strings (props interface and logic are unchanged):

Card wrapper (outermost div): change `"md:col-span-1 p-4 border rounded-md bg-gray-50"` to:
`"md:col-span-1 bg-white border border-gray-200 rounded-md shadow-sm p-4"`

Section heading (h2): change `"text-xl font-semibold mb-4 text-blue-700"` to:
`"text-xl font-semibold mb-4 text-gray-800 border-b border-gray-200 pb-2"`

Each checkbox `<input type="checkbox">`: change from `"mr-2"` to `"w-4 h-4 accent-blue-600 cursor-pointer"`

Each checkbox `<div className="flex items-center">`: keep as `"flex items-center gap-2"`

Each checkbox `<label>`: change to `"text-sm text-gray-700 cursor-pointer"`

Disabled state label for HMR: change `className={pharmacistIds.includes(classification) ? "" : "text-gray-500"}` to `className={pharmacistIds.includes(classification) ? "text-sm text-gray-700 cursor-pointer" : "text-sm text-gray-400 cursor-not-allowed"}`

All number `<input type="number">` fields (mealAllowance, mealAllowanceExtra, motorVehicleKm): change from `"w-full p-2 border rounded"` to:
`"w-full p-3 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-transparent placeholder-gray-400 text-gray-700"`

All `<label>` for number inputs: change from `"block text-gray-700 mb-1"` to `"block text-sm font-medium text-gray-700 mb-1"`

The `<div className="space-y-3">` inner wrapper: keep as `"space-y-4"` (increase from 3 to 4 for better spacing).
  </action>
  <verify>
    <automated>npm test -- --watchAll=false --testPathPattern="(EmployeeDetails|Allowances).test.js" 2>&1 | tail -20</automated>
  </verify>
  <done>EmployeeDetails.test.js and Allowances.test.js all pass; EmployeeDetails.js contains "focus:ring-2 focus:ring-blue-500" on select and input elements; Allowances.js contains "accent-blue-600" on checkboxes; neither file contains old class names (.button, md:col-span-1 p-4 border rounded-md bg-gray-50 is replaced).</done>
</task>

</tasks>

<verification>
After both tasks:
1. Run full test suite: `npm test -- --watchAll=false` — all tests must pass
2. Start `npm start` and verify: award selector has a white card panel with label, select, and "Refresh Rates" button in a flex row
3. EmployeeDetails and Allowances panels are white cards with gray borders and shadows
4. All form inputs have visible border and a blue ring on focus (tab through the form to verify)
5. On a 375px viewport: EmployeeDetails and Allowances stack vertically (1 column)
</verification>

<success_criteria>
1. `npm test -- --watchAll=false` exits 0
2. AwardSelector.js contains no .award-selector BEM classes (grep: `award-selector__`)
3. AwardSelector.js does not render error state (grep: `award-selector__error` returns nothing; no error div JSX)
4. EmployeeDetails.js contains `focus:ring-2 focus:ring-blue-500` on select and number input
5. EmployeeDetails.js card wrapper contains `md:col-span-2`
6. Allowances.js card wrapper contains `md:col-span-1`
7. Allowances.js checkboxes contain `accent-blue-600`
</success_criteria>

<output>
After completion, create `.planning/phases/02-tailwind-css-redesign/02-02-SUMMARY.md` using the summary template.
</output>
