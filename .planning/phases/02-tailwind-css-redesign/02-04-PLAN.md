---
phase: 02-tailwind-css-redesign
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified: []
autonomous: false
requirements:
  - UX-01
  - UX-02

must_haves:
  truths:
    - "Human has confirmed the app looks professional with navy header and white content panels"
    - "Human has confirmed forms are usable on a 375px viewport (mobile)"
    - "Human has confirmed WorkHours table scrolls horizontally on mobile without breaking layout"
    - "Human has confirmed green/red/yellow status badges are visible and correct"
    - "Human has confirmed weekly summary row appears only after entering actual paid data"
    - "All automated tests pass (0 failures)"
  artifacts:
    - path: ".planning/phases/02-tailwind-css-redesign/02-04-SUMMARY.md"
      provides: "Checkpoint sign-off record"
  key_links:
    - from: "browser at localhost:3000"
      to: "phase success criteria"
      via: "human visual and interactive verification"
      pattern: "manual"
---

<objective>
Human verification checkpoint for Phase 02 Tailwind CSS Redesign. Confirms the full suite of automated tests pass, visual design matches the navy/white spec, forms are mobile-usable at 375px, and status indicators work correctly.

Purpose: Styling changes cannot be fully verified by automated tests. This checkpoint ensures the intended visual and interactive experience is correct before Phase 02 is marked complete and Phase 03 begins.

Output: Human sign-off on all Phase 02 success criteria. No code changes expected; this plan records the approval decision.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-tailwind-css-redesign/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and start dev server</name>
  <files><!-- No files modified — this task reads test files to verify they exist, then runs the test suite --></files>
  <read_first>
    - C:/Users/josha/pay-check-app/src/App.test.js (confirm all new tests are present)
    - C:/Users/josha/pay-check-app/src/components/OverviewBreakdown.test.js (confirm weekly summary tests present)
    - C:/Users/josha/pay-check-app/src/components/AwardSelector.test.js (confirm dismiss test present)
  </read_first>
  <action>
Run the full test suite:

```bash
npm test -- --watchAll=false
```

All tests must exit 0. If any tests fail, diagnose and fix before proceeding to the checkpoint task. Do not mark this task complete if there are test failures.

After tests pass, start the dev server:

```bash
npm start
```

The browser should open at http://localhost:3000.
  </action>
  <verify>
    <automated>npm test -- --watchAll=false 2>&1 | tail -10</automated>
  </verify>
  <done>npm test exits 0 with 0 failures; dev server starts and serves the app at localhost:3000.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and responsive verification</name>
  <what-built>
Full Tailwind CSS redesign of all app components:
- Navy header (slate-900) with "Pay Check App" title and static subtitle
- Dismissible red error banner (below header, only when API fails)
- Loading overlay with spinner (during rate fetch)
- AwardSelector, EmployeeDetails, Allowances as white card panels with consistent input styling
- WorkHours table with horizontal scroll on small screens
- OverviewBreakdown with green/red/yellow status badges and weekly summary row
- ImportantNotes section styled (not a stub)
  </what-built>
  <how-to-verify>
Open http://localhost:3000 in a browser. Complete these checks in order:

**1. Header (D-01, D-02, D-03)**
- [ ] Header background is dark navy (not gray or white)
- [ ] Title reads "Pay Check App" (not "Pharmacy Industry Award Pay Calculator")
- [ ] Subtitle reads "Check if you're being paid correctly"
- [ ] Header padding is compact (approximately 16px top/bottom — not a tall hero section)
- [ ] Award selector appears BELOW the header as a separate section (D-04)

**2. Loading state (D-08)**
- [ ] On first page load, a semi-transparent overlay with a spinning loader appears over the content
- [ ] The navy header remains visible above the overlay during loading
- [ ] After rates load, the overlay disappears and the form is accessible

**3. Error banner (D-09, D-10)**
- [ ] To test: disconnect the internet or temporarily break the API key, then hard-refresh
  - OR: In browser DevTools → Network tab → check "Offline" checkbox, then refresh
- [ ] A red banner appears below the header with title "Unable to Load Award Rates"
- [ ] An × button is visible on the right side of the banner
- [ ] Clicking × removes the banner from the page
- [ ] Reconnect internet before continuing

**4. Form layout (D-05, D-07)**
- [ ] EmployeeDetails and Allowances panels appear side by side on desktop (EmployeeDetails wider)
- [ ] All section cards have white background, visible border, and shadow
- [ ] Section order: AwardSelector → EmployeeDetails+Allowances → WorkHours → OverviewBreakdown → Important Notes → Footer

**5. Mobile layout at 375px (D-05, D-06)**
Open DevTools → Toggle Device Toolbar → select "iPhone SE" (375px):
- [ ] EmployeeDetails and Allowances stack vertically (1 column, not side by side)
- [ ] All form inputs are usable (not cut off, not too small to tap)
- [ ] WorkHours table scrolls HORIZONTALLY — the table does not break, inputs are not squished
- [ ] Calculate button is visible and tappable

**6. Status indicators (D-11, D-12, D-13)**
Close DevTools responsive mode and return to desktop view.
- Select Pharmacy Industry Award, Full-time, PA Level 1
- Enter Monday 09:00 → 17:00 (8 hours ordinary weekday shift)
- Click "Calculate Pay"
- [ ] OverviewBreakdown table renders with a Monday row
- [ ] The "Actual Paid" column shows an empty number input with no status badge
- [ ] Weekly Summary section is ABSENT (completely hidden — D-13)
- Enter "200.00" in the Monday Actual Paid input (use approximately the calculated amount)
- [ ] Weekly Summary section APPEARS with "Weekly Summary" heading (D-12)
- [ ] Status badge is green "OK" if amount matches, red "Underpaid" if too low, yellow "Overpaid" if too high
- Enter an amount lower than calculated (e.g., "100.00")
- [ ] Monday row shows red "Underpaid" badge
- [ ] Weekly Summary shows red "Underpaid" badge
- Click the Monday row to expand the accordion
- [ ] Segment detail table appears with Time, Rate Type, Amount columns

**7. Typography and colour**
- [ ] Page title is large (approximately 28px bold)
- [ ] Section headings are medium (approximately 20px semibold)
- [ ] Form labels are smaller (approximately 14px)
- [ ] Form inputs have visible gray border and a blue ring on keyboard focus (Tab through form)
  </how-to-verify>
  <resume-signal>
Type "approved" if all checks pass.

If issues found, describe which checks failed (e.g., "error banner not showing" or "WorkHours not scrolling at 375px") and Claude will fix them before re-testing.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 02 is complete when:
1. `npm test -- --watchAll=false` passes (0 failures)
2. Human types "approved" after completing all visual/responsive checks above
3. All 5 Phase 02 success criteria from ROADMAP.md are satisfied:
   - All components use Tailwind utility classes
   - Navy header/white content palette
   - Green/red/yellow status indicators
   - Forms usable on mobile (≥375px)
   - All existing functionality preserved
</verification>

<success_criteria>
1. `npm test -- --watchAll=false` exits 0
2. Human approves all 7 visual/responsive check groups
3. No regression in calculation logic (weekly pay cycle test passes)
</success_criteria>

<output>
After human approval, create `.planning/phases/02-tailwind-css-redesign/02-04-SUMMARY.md` using the summary template.

Then update `.planning/STATE.md`:
- current_phase: Phase 03 — Polish
- Update progress: Phase 02 complete

Update `.planning/ROADMAP.md`:
- Mark Phase 02 checkbox as [x]
- Update Plans count from "0/?" to "4/4 plans complete"
</output>
