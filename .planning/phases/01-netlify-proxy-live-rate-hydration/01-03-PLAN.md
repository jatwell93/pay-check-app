---
phase: 01-netlify-proxy-live-rate-hydration
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified: []
autonomous: false
requirements: [PROXY-01, PROXY-02, PROXY-03, UX-03]

must_haves:
  truths:
    - "App loads in netlify dev without any CORS errors in browser console"
    - "A loading state is visible before rates are fetched (spinner or disabled button)"
    - "After rates load, Calculate button becomes enabled"
    - "When the proxy is unreachable, an error banner appears — not a blank screen"
    - "When fallback is active, Calculate button is still enabled and produces a calculation"
    - "All 61 tests pass (confirmed in Plan 02 — this plan verifies the live runtime)"
  artifacts:
    - path: "netlify/functions/award-rates.js"
      provides: "Running function visible at http://localhost:8888/.netlify/functions/award-rates"
    - path: "netlify.toml"
      provides: "Build config serving from ./build with functions at ./netlify/functions"
  key_links:
    - from: "browser fetch"
      to: "netlify/functions/award-rates.js"
      via: "http://localhost:8888/.netlify/functions/award-rates"
      pattern: "200 OK"
---

<objective>
Human-verify that the full proxy flow works end-to-end in the netlify dev environment: CORS-free, loading states visible, fallback banner shown on error.

Purpose: Automated tests mock fetch and cannot confirm the real CORS bypass or the visual loading/error states. A single manual verification pass catches deployment issues before they reach production. This checkpoint gate ensures Phase 1 success criteria are fully met before /gsd:verify-work.

Output: Human confirmation that the proxy works, or a list of issues to fix.
</objective>

<execution_context>
@C:/Users/josha/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/josha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-netlify-proxy-live-rate-hydration/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start netlify dev and confirm proxy endpoint responds</name>
  <files></files>
  <action>
Start the Netlify dev server (which serves both the React app AND the Netlify Functions):

```bash
npx netlify dev
```

This starts the app at http://localhost:8888 (not 3000). Both the CRA React app and the /.netlify/functions/ endpoints are available.

Then make a test curl to confirm the proxy function loads (even without a valid API key, it should return a 500 with a clear error message, not a crash):

```bash
curl -s "http://localhost:8888/.netlify/functions/award-rates?awardIds=MA000012" | head -c 200
```

Expected: JSON response (even if `{ "error": "API key not configured on server" }` — this confirms the function loaded and ran). If the function crashed (unhandled exception), curl returns nothing or HTML. That would be a bug.
  </action>
  <verify>
    <automated>curl -s "http://localhost:8888/.netlify/functions/award-rates?awardIds=MA000012" 2>/dev/null | python3 -c "import sys,json; d=json.load(sys.stdin); print('JSON OK:', list(d.keys())[:3])" 2>&1 || echo "Function not responding — ensure netlify dev is running"</automated>
  </verify>
  <done>curl returns valid JSON from the proxy endpoint (either rates data or a structured error object — both confirm the function is running)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify end-to-end proxy flow</name>
  <action>
Verify the following 5 tests manually with the app running.

**Test 1 — Loading state (UX-03)**
With `npx netlify dev` running at http://localhost:8888:
1. Open http://localhost:8888 in Chrome, open DevTools Network tab
2. Hard refresh (Ctrl+Shift+R)
3. Expected: Calculate Pay button is disabled/greyed out briefly while rates fetch
4. Expected: After 1-3 seconds, button becomes active
5. Expected: No CORS errors in Console tab

**Test 2 — No CORS errors (PROXY-01)**
1. In DevTools Console tab, confirm no "Access-Control-Allow-Origin" or "CORS" errors
2. In Network tab, find the request to `/.netlify/functions/award-rates` — status 200 (key set) or JSON error (no key)

**Test 3 — Fallback error banner (PROXY-03)**
1. Kill netlify dev (Ctrl+C), run `npm start` instead (port 3000, no functions)
2. Open http://localhost:3000
3. Expected: Error banner appears with text like "Couldn't load award rates"
4. Expected: Calculate Pay button is NOT disabled after fallback kicks in
5. Expected: No blank screen or unhandled errors

**Test 4 — Calculate with fallback rates (PROXY-02 fallback)**
1. While on http://localhost:3000 with the error banner showing:
2. Select Pharmacy Industry Award, Pharmacy Assistant Level 1, Full-time
3. Enter Monday: 09:00 to 17:00, click Calculate Pay
4. Expected: Results appear showing approximately $207.92 (8h at $25.99 ordinary rate)
5. Expected: This confirms calculatePay is using fallback rates, not broken

**Test 5 — Regression**
Run: `npm test -- --watchAll=false` — Expected: 0 failures
  </action>
  <verify>
    <automated>cd "C:\Users\josha\pay-check-app" && npm test -- --watchAll=false 2>&1 | tail -5</automated>
  </verify>
  <done>Human types "approved" — all 5 tests confirmed passing</done>
  <what-built>
    Plan 01: Netlify Function proxy (netlify/functions/award-rates.js) + netlify.toml
    Plan 02: awardRatesService redirected to proxy, calculatePay reads live rates, Calculate button disabled during load
  </what-built>
  <resume-signal>Type "approved" if all tests pass, or describe specific failures to fix</resume-signal>
</task>

</tasks>

<verification>
Phase 1 is complete when:

1. Human has approved the checkpoint above
2. `npm test -- --watchAll=false` passes (0 failures)
3. No CORS errors visible in browser console when running via netlify dev
4. Error banner visible when app loads without proxy (fallback state)
5. Calculate button disabled during loading, enabled after rates resolve
</verification>

<success_criteria>
- PROXY-01: App loads via netlify dev with no CORS errors; /.netlify/functions/award-rates responds with JSON
- PROXY-02: calculatePay uses live rates when available; confirmed by calculation producing correct output
- PROXY-03: Error banner visible when proxy unavailable; calculation still works with fallback rates
- UX-03: Calculate button disabled during load; enabled after; error states clearly visible
- All 61+ tests pass (0 failures)
</success_criteria>

<output>
After completion, create `.planning/phases/01-netlify-proxy-live-rate-hydration/01-03-SUMMARY.md`
</output>
